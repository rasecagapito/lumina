# CHECKPOINT_20260528_1606

## Contexto

Sessão de deploy da API HOM no Coolify. Continuação do checkpoint anterior (CHECKPOINT_20260528_1239.md).

---

## O que foi feito

- API HOM deployada com sucesso no Coolify via Dockerfile
- Resolvidos 5 erros sequenciais de build/runtime:
  1. Nixpacks não detectava monorepo → trocado para Dockerfile
  2. Base Directory incorreta → corrigido para `/` (raiz)
  3. Testes incluídos no `tsc` build → excluídos via tsconfig
  4. ESM sem extensão `.js` nos imports → trocado para CommonJS
  5. Node 20 sem WebSocket nativo → upgrade para Node 22
- Variáveis de ambiente configuradas no Coolify (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, JWT_SECRET, PORT)
- DNS `api-hom.hagap.online` criado na Hostinger → `2.24.85.10`
- Domínio configurado com `https://` no Coolify para gerar labels Traefik corretos
- `GET /health` retorna `{"status":"ok"}` ✅
- Lixo Next.js commitado pelo GPT anterior removido do repo
- Histórico de branches divergentes resolvido via `--allow-unrelated-histories`
- Remote `origin` configurado no ambiente Bash

---

## Arquivos alterados

| Arquivo | Alteração |
|---------|-----------|
| `apps/api/Dockerfile` | Criado — build monorepo, Node 22 |
| `apps/api/tsconfig.json` | CommonJS output, excluir testes |
| `apps/api/package.json` | Removido `"type": "module"` |
| `apps/api/src/server.ts` | Import revertido para sem extensão |
| `.gitignore` | `.env.*` e `.env_*` confirmados |
| `PROD.md` | Atualizado (esta sessão) |
| `CONFIG.md` | Atualizado (esta sessão) |

---

## Estado da infraestrutura

| Item | Status |
|------|--------|
| Supabase HOM (`znpqxsuxcwwfafztjxya`) | ✅ Ativo + migrations |
| Supabase PRD self-hosted (`supabase.hagap.online`) | ✅ Disponível |
| API HOM (`api-hom.hagap.online`) | ✅ Online — `{"status":"ok"}` |
| Proteções branch `main` e `hom` | ✅ Ativas |
| Secrets GitHub Actions HOM (5) | ✅ Configurados |
| Secrets GitHub Actions PRD (5) | ✅ Configurados |
| SSL `api-hom.hagap.online` | ⚠️ Pendente (Coolify emitindo) |
| Vercel HOM (frontend) | ⏳ Pendente |
| API PRD (Coolify PRD) | ⏳ Pendente |
| `RAILWAY_HOM_TOKEN` / `API_HOM_URL` GitHub Secret | ⏳ Pendente (Railway substituído por Coolify) |

---

## Decisões tomadas

- Railway substituído por Coolify para deploy da API (já temos VPS)
- API usa CommonJS (não ESM) para evitar problemas de extensão em imports
- Node 22 no Dockerfile para WebSocket nativo do Supabase
- Dockerfile na raiz do contexto (`/`) para acesso ao monorepo completo
- Domínio Coolify deve ter `https://` para Traefik gerar labels corretos

---

## Pendências

- SSL automático do Coolify ainda processando para `api-hom.hagap.online`
- Secrets `RAILWAY_HOM_TOKEN` e `API_HOM_URL` obsoletos — substituir por `COOLIFY_HOM_WEBHOOK` e `API_HOM_URL=https://api-hom.hagap.online` no GitHub Actions
- `deploy-hom.yml` precisa ser atualizado: remover Railway CLI, usar webhook Coolify
- Vercel HOM para o frontend
- Brainstorming Fase 1 (Auth+Membros)

---

## Próximos passos

1. Aguardar SSL automático do Coolify
2. Atualizar `deploy-hom.yml` — trocar Railway por webhook Coolify
3. Configurar secret `API_HOM_URL=https://api-hom.hagap.online` no GitHub
4. Configurar Vercel HOM para o frontend
5. Brainstorming Auth+Membros (Fase 1)
