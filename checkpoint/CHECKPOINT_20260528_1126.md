# CHECKPOINT_20260528_1126

## Contexto

Sessão de provisionamento de infraestrutura HOM e configuração de governança do projeto.

---

## O que foi feito

- Supabase HOM provisionado: projeto `lumina-hom` (`znpqxsuxcwwfafztjxya`) na org Lumina, região `sa-east-1`
- 8 migrations SQL aplicadas via `npx supabase db push` (001→008)
- `.env` corrigido com variáveis corretas:
  - `SUPABASE_URL` (API)
  - `SUPABASE_SERVICE_ROLE_KEY` (API)
  - `VITE_SUPABASE_URL` (frontend)
  - `VITE_SUPABASE_ANON_KEY` (frontend)
- Política de branches documentada em `PROD.md` e `CONFIG.md`
- GitHub CLI (`gh`) instalado via winget v2.93.0 e autenticado como `rasecagapito`

---

## Arquivos alterados

| Arquivo | Alteração |
|---------|-----------|
| `.env` | Variáveis Supabase HOM corrigidas e organizadas |
| `PROD.md` | Seção 3 atualizada: repo URL, status HOM, política de branches |
| `CONFIG.md` | Seção 10 reescrita com política de branches completa |

---

## Pendente desta sessão

- Proteções de branch `main` e `hom` no GitHub **não aplicadas ainda**
  - `gh` instalado e autenticado, mas PATH não disponível no PowerShell do Claude
  - Executar **no terminal do usuário** (onde `gh auth login` foi feito):

```powershell
# Proteção branch main
gh api repos/rasecagapito/lumina/branches/main/protection --method PUT --field required_status_checks=null --field enforce_admins=true --field "required_pull_request_reviews[required_approving_review_count]=1" --field "required_pull_request_reviews[dismiss_stale_reviews]=true" --field restrictions=null --field allow_force_pushes=false --field allow_deletions=false

# Proteção branch hom
gh api repos/rasecagapito/lumina/branches/hom/protection --method PUT --field required_status_checks=null --field enforce_admins=false --field required_pull_request_reviews=null --field restrictions=null --field allow_force_pushes=false --field allow_deletions=false
```

---

## Estado da infraestrutura

| Item | Status |
|------|--------|
| Supabase HOM (`znpqxsuxcwwfafztjxya`) | ✅ Provisionado + migrations aplicadas |
| Supabase PRD | ⏳ Pendente |
| Secrets GitHub Actions | ⏳ Pendente |
| Proteções de branch (`main`, `hom`) | ⏳ Pendente (comandos prontos acima) |
| Railway HOM | ⏳ Pendente |
| Vercel HOM | ⏳ Pendente |

---

## Próximos passos

1. Aplicar proteções de branch via terminal (`gh api ...` acima)
2. Configurar secrets GitHub Actions:
   - `SUPABASE_HOM_URL`, `SUPABASE_HOM_ANON_KEY`, `SUPABASE_HOM_SERVICE_ROLE_KEY`, `SUPABASE_HOM_PROJECT_REF`
   - `SUPABASE_ACCESS_TOKEN`, `RAILWAY_HOM_TOKEN`, `API_HOM_URL`
3. Provisionar Supabase PRD
4. Configurar Railway HOM (API deploy)
5. Configurar Vercel HOM (frontend deploy)
